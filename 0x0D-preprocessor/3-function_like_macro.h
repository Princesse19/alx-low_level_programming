#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
/**
 * ABS - Compute the absolute value of a number x
 * @x: the exact number to give the absolute value of
 *
 * Return: the absolute value of x
 */
#define ABS(x) ((x) < 0 ? -(x) : (x))

#endif
